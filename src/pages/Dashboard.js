import React from 'react';
import { Typography, Box } from '@mui/material';

const Dashboard = () => (
  <Box>
    <Typography variant="h4">Drone AI Platform Dashboard</Typography>
  </Box>
);

export default Dashboard;