import React from 'react';
import { Typography, Box } from '@mui/material';

const MunicipalServices = () => (
  <Box>
    <Typography variant="h6">Automated Municipal Services</Typography>
    <Typography>Digital challan generation and integration with revenue system.</Typography>
  </Box>
);

export default MunicipalServices;